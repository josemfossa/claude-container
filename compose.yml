services:
  # Service with auth and workspace mounts
  # Files excluded by .claudeignore won't be included in the build context
  claude-auth-workspace:
    build:
      context: ${CONTEXT}
      dockerfile: ${CLAUDE_CONTAINER_DIR}/Dockerfile.claude
    stdin_open: true
    tty: true
    userns_mode: keep-id
    #profiles:
    # Check this
    #- auth-workspace
    volumes:
      - ${CLAUDE_CONFIG_DIR:-~}/.claude.json:/home/node/.claude.json
      - ${CLAUDE_CONFIG_DIR:-~}/.claude:/home/node/.claude
      - ${CLAUDE_CONFIG_DIR:-~}/.claudeconfig:/home/node/.config/anthropic/
      - ${CONTEXT}:/workspace
    working_dir: /workspace
